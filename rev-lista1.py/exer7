v = input().split()
a = float(v[0])
b = float(v[1])
c = float(v[2])
delta = b**2 - 4*a*c
if delta < 0 or a == 0:
  print('impossivel calcular')
else:
  x1 = (-b + delta**0.5)/(2*a)
  x2 = (-b - delta**0.5)/(2*a)
  print(f'As raizes são {x1} e {x2}')
  
